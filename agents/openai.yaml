interface:
  display_name: "Screenshot-Guided Business Verification"
  short_description: "Turn requirements into executable business UI tests"
  default_prompt: "Use $screenshot-guided-business-verification to create a requirement-driven business verification suite before implementing this feature."

policy:
  allow_implicit_invocation: true
