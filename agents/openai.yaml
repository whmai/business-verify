interface:
  display_name: "Business Verify"
  short_description: "Turn requirements into executable business UI tests"
  default_prompt: "Use $business-verify to create a requirement-driven business verification suite before implementing this feature."

policy:
  allow_implicit_invocation: true
