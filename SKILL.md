---
name: screenshot-guided-business-verification
description: "自测通过、自测、功能验收、业务验证：将需求、文字步骤或截图转为可执行的 UI、接口和数据验证集；不用于仅做静态代码审查。"
---

# 截图与文字引导的业务验证

将测试人员的示范视为业务知识，不是坐标回放素材。主目标是：**从需求先生成一份可追溯的指导测试验证集，再以它作为 AI 开发、自测和验收的门禁。** 测试人员能按其逐步操作，AI 能在受控环境执行，结果由页面、接口和已获授权的数据断言共同判定。

## 适用输入与边界

输入可以是需求文档、接口资料、业务规则、缺陷描述、纯文字测试步骤，或带文字说明的截图。**没有截图时，必须优先采用“文字优先模式”；截图只是可选的步骤证据。** 当输入只有需求时，仍必须先输出验证集草稿，不能直接跳到实现代码。

不要用本 Skill 在生产环境创建、修改或删除业务数据，除非用户逐项明确授权。默认只在本地、测试或隔离预发环境执行。没有环境、账号或数据权限时，可以交付测试契约和待执行清单，但不能声称测试已通过。

截图、录制数据、接口响应和数据库查询可能含个人信息或凭据。只保留完成验证所需的最小证据，先脱敏；不得将原始生产快照、密码、令牌或完整敏感响应提交到仓库。

## 工作方式

### 1. 从需求生成指导测试验证集（先于开发）

将每一条可观察的需求拆成带稳定编号的验收项，例如 `R-01`。使用 [需求到验证集模板](references/requirements-to-verification-set.md) 输出以下内容：

- 需求—案例映射：每项需求至少由一个正向案例覆盖；金额、权限、状态、异常和幂等性等高风险规则增加边界或反向案例；
- 一份按真实测试人员操作顺序编写的文字脚本：入口、观察、点击、输入、即时预期、跨页面复核、接口/数据核验和证据；
- 每个步骤的确定性前置数据、动态字段比较规则、通过标准与清理方式；
- 不能从需求推导的 UI 名称、业务口径、接口或权限，统一列为待确认项，不得编造。

这份文档就是本次需求的**指导测试验证集**。提交给用户/业务/测试负责人审阅关键口径后，保存到项目约定的测试目录并关联需求编号。截图、录屏和人工执行结果到来后，补充或修正对应步骤，而不是替换需求追踪关系。

### 2. 建立测试上下文

在写步骤前确定：被测功能与业务目标、执行环境、测试角色、允许的副作用、测试数据来源/版本、逻辑业务时间、外部依赖和清理策略。金额口径、状态机、权限和副作用等关键规则若不明确，应列为待确认，不能由 AI 从页面现象猜测。

### 3. 将输入转为拟人化测试脚本

使用 [文字优先案例模板](references/text-first-case-template.md) 为每个业务目标建立独立案例。步骤必须像专业测试人员实际执行那样具体，依次写清：

1. 从哪里进入：URL、首页菜单路径或上一步的页面；
2. 看什么：页面标题、当前状态、可见控件或记录；
3. 做什么：点击哪个具有业务名称的控件，输入或选择什么值；
4. 当场看到什么：成功提示、校验错误、加载状态或新记录；
5. 去哪里复核：详情页、列表页、关联页面、接口或数据层；
6. 为什么算正确：明确页面、接口或业务数据断言；
7. 留下什么证据：截图区域、请求 ID、脱敏响应摘要或失败记录。

不能只写“测试退款功能”或“检查结果正确”。也不能虚构需求中没有出现的按钮、URL、字段、状态或接口。信息缺失时写成 `待确认`，并询问最小必要问题。

用用户语义定位控件，例如“点击可见名称为‘提交退款’的按钮”。自动化时优先使用稳定测试标识、可访问角色和名称、唯一可见文本、结构化定位；坐标点击仅可作为一次性人工辅助，不能成为长期回归脚本的唯一定位方式。

### 4. 让数据变化不破坏验证

依据 [断言目录](references/assertion-catalog.md) 选择真值来源。不要把实时业务库中某条记录的固定字面量当成唯一预期：

- PR 回归使用迁移脚本加最小种子数据，或已脱敏、已版本化的快照；固定业务时钟和随机种子。
- 动态订单号、UUID 和时间只校验格式、存在性、关联关系、范围或前后差值，并明确哪些字段被忽略及原因。
- 金额、库存、状态和跨系统结果必须写为不变量、状态机、对账关系或自定义查询；HTTP 200 或成功弹窗本身不充分。
- 重构和迁移在同一快照或同一 CDC/事务水位上比较新旧实现；不要分别读取不断变化的生产数据再比较。
- 异步流程等待业务终态或事件结果，设定可解释的超时；不能以固定 sleep 代替。

### 5. 基于验证集开发、自测和报告

先把案例草稿交给业务/测试负责人确认，尤其是金额、状态、权限、异常与幂等性。开发时，把每一项验收项及其案例视为待通过门禁：先让至少一项相关自动化/可执行检查在未实现时失败，再实现功能直至通过；不得只编写“总是通过”的测试或借由 mock 掩盖被测业务逻辑。然后：

1. 记录环境、用例版本、数据版本与业务时间；创建或确认隔离数据。
2. 逐步执行 UI 操作，在有意义的页面或状态变化处截图取证。
3. 按契约检查 UI、接口，以及已明确获授权的数据库、消息或日志证据。
4. 所有硬性结论都要可机械判定。LLM 可帮助理解截图和诊断，但不能单独充当金额、权限或状态机正确性的裁判。
5. UI 改版或定位失败时报告候选修复和影响；不得静默放宽断言、改写预期或把失败标记为通过。
6. 依照 [报告模板](references/report-template.md) 输出通过、失败、部分通过或未执行；未执行项要明确原因。

报告必须逐项回填需求编号、案例编号和证据。只有所有硬性案例通过，且不存在未解释的高风险未执行项，才能称“自测通过”；否则明确说明未覆盖、失败或受环境限制的范围。

### 6. 失败后的自主修正与回测闭环

任何硬性案例失败时，不能停在“发现问题”的报告，也不能只修改预期、删除断言、扩大容差或用 mock 掩盖被测逻辑。应执行以下闭环：

1. 保存失败证据，并将失败归类为实现缺陷、测试/夹具缺陷、环境/外部依赖故障，或需求歧义。
2. 属于实现缺陷且在用户授权的开发范围内时，自行定位并修正代码或配置；测试/夹具确有错误时，只有在不改变已确认业务预期的前提下才能修正。
3. 用干净的隔离数据或同一版本夹具重新执行失败案例；修复后先回测失败案例，再运行所有受影响案例及全部 P0 硬性案例。
4. 若仍失败，继续“诊断 → 修正 → 回测”，直到所有硬性自测案例通过；每一轮保留最小必要的失败和通过证据。
5. 将经确认的真实缺陷路径保留为回归案例，防止同类问题再次通过。

以下情况不能伪造“全部通过”：需求或正确结果不明确、所需权限/测试环境/外部系统不可用、修复将超出用户授权范围，或测试本身与已确认业务契约冲突。此时停止修改相关结论，报告已验证的事实、阻塞原因和需要用户确认的最小事项；其他不受阻塞的案例仍应继续完成。

### 7. 最终交付门禁

Skill 被调用来开发或验证功能时，生成的指导测试验证集就是最终交付门禁。AI 必须先按验证集完成自测；发现实现问题后必须自行修复并回测，直到全部硬性案例通过，才可以向用户宣称功能完成、请求验收或交付最终结果。最终报告必须列出需求—案例结果、修复轮次和最后一次回测证据。任何未执行、失败或被阻塞的硬性案例都意味着“未完成”，不能隐藏在交付结论之后。

## 维护回归资产

将确认后的案例、最小测试数据定义和自动化实现放进项目约定的测试目录。案例名称应表达业务结果，而不是坐标或 CSS 路径。新缺陷先在隔离环境复现；经脱敏和业务确认后，加入回归集。实现者不得在无独立审阅的情况下同时改业务预期和功能实现，然后自行宣布通过。
